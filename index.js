import React from "react";
import ReactDOM from "react-dom";
import HomePage from "./internship_platform_home";

ReactDOM.render(
  <React.StrictMode>
    <HomePage />
  </React.StrictMode>,
  document.getElementById("root") // Ensure index.html has <div id="root"></div>
);
